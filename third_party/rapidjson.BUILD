# rapidjson — header-only JSON parsing library
# Pinned to commit ab1842a (post v1.1.0, with C++11+ and bug fixes)

cc_library(
    name = "rapidjson",
    hdrs = glob(["include/**/*.h"]),
    includes = ["include"],
    visibility = ["//visibility:public"],
)
