using System;

// Generator-output uses `using Atlas.Core;` + `[ModuleInitializer]`; under
// netstandard2.1 / Unity neither is in scope without these stubs.

namespace Atlas.Core
{
    internal sealed class _NamespaceAnchor { private _NamespaceAnchor() { } }
}

#if !NET5_0_OR_GREATER
namespace System.Runtime.CompilerServices
{
    [AttributeUsage(AttributeTargets.Method, Inherited = false)]
    public sealed class ModuleInitializerAttribute : Attribute { }
}
#endif
