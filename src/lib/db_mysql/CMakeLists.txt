# atlas_db_mysql — MySQL database backend (placeholder)

if(ATLAS_DB_MYSQL)
  # TODO: add MySQL backend sources when implemented
  # add_library(atlas_db_mysql STATIC ...)
  # target_link_libraries(atlas_db_mysql PUBLIC atlas_db_iface ...)
endif()
